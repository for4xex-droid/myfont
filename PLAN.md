# MyMincho 制作計画書 v7

自作明朝体フォント（常用漢字 2,136 字＋仮名・英数・約物 ≒ 2,500 字）と、その設計を支える「フォント要素データベース（fontdb）」の統合計画。
2026-08-09 作成。レッドチーム検証（v2）、実コードスパイク＋車輪の再発明監査（v3）、KAGE実地＋端到端検証（v4）、fontdb MVP実証＋黒み減衰カーブ検証＋文書監査（v5）、交差ソルバ実装＋KAGE×エンジン結合検証（v6）、事業戦略整合（リリース/マネタイズ→§7、v7）を反映。

関連文書:
- `GOLDENRULES.md` — 実装の鉄の掟（全作業で遵守）
- `docs/strategy.md` — 事業戦略の正本（ポジショニング・リリースフェーズ・マネタイズ階層・KPI）
- `docs/quality_to_alpha_plan.md` — P1 盲検後の品位→α 正本（荒いクローズ → 字数 → 本盲検）
- `WORKFLOW.md` — 日常の作業ループ3種の定義
- `prototype/REPORT.md` — ストローク合成プロトタイプの検証結果
- `spike/SPIKE_REPORT.md` — pathops union・freetype計測・ビルドチェーンの検証
- `spike2/SPIKE2_REPORT.md` — KAGEダンプ・常用漢字リスト・写像難易度の実地検証
- `spike3/SPIKE3_REPORT.md` — union→UFO→OTF→計測の端到端検証（物差し1本化の実証）
- `spike4/SPIKE4_REPORT.md` — fontdb MVP 縮小実装（5書体取得・probe安定性・散布図）
- `spike5/SPIKE5_REPORT.md` — 黒み減衰カーブの実測＋文書整合性監査
- `spike6/SPIKE6_REPORT.md` — 交差ソルバ Stage A+B の実装検証（期待contour到達・回帰テスト雛形）
- `spike7/SPIKE7_REPORT.md` — KAGE骨格×エンジン肉付けの結合検証（P4a→P3接続・100字スケール実測）

---

## 0. 確定済みの基本方針

- 制作方式: **パラメトリック・ディテール規則**を核に、**スケルトン（骨格＋端点タグ）＋肉付けエンジン**で量産。部品（部首）合成＋文脈変形を併用
- 黒み調整は**密度ヒューリスティック＋例外リスト**。機械学習は下書き補助のみ。**v5 で前提を修正**: 「画数が多いほど線を一律に細くする」減衰は実フォント（Noto Serif JP）で観測されなかった（結合線幅 α≈0.04 でほぼ平坦。縦画のみ弱い減衰 α≈0.07）。実フォントは線幅をほぼ維持したまま密度上昇を許容している（ink密度は画数と r≈+0.76 で上昇）。よって P5 は「画数→線幅の一律減衰式」ではなく、**密度の実測検知＋例外リスト＋縦画のみの弱い減衰（初期値 α≈0.07）**を主とする
- 仮名約 170 字＋基準漢字 50〜100 字は**手設計必須**（書体の印象の核）
- 骨格データは **GlyphWiki KAGE ダンプ**を流用。KanjiVG は筆順・部品分割の参照のみ（形状は混入禁止）
- 計測対象は**オープンライセンスのみ**（源ノ明朝・IPAex明朝・しっぽり明朝・Zen Old Mincho・BIZ UD明朝）。輪郭トレース転用は禁止、数値計測のみ
- **v1 は横書きのみ**。縦書き（`vert`/`vrt2`）は v2 以降の明示的スコープとする
- 検証済みの技術的事実: ストローク合成（うろこ・打ち込み・はね・はらいのパラメータ生成）は機能する。**交差接合・単一輪郭化が未実装**で「永」が破綻した（→ P2 が最大の技術リスク）
- スパイクで実証済み（`spike/` `spike2/` `spike3/`）:
  - skia-pathops の union は交差の融合に有効（十: 6→2 contour、永: 13→4 contour）。ただし **union 単体では単一輪郭化しない**（点画の非接触、打ち込み由来の 10〜30 ユニット微小島が残る）→ Stage A の食い込み＋微小輪郭除去が必須
  - freetype-py 計測パスは動作する。Noto Serif JP Regular の「十」実測でコントラスト比 **≈2.41**（校正アンカーとして利用可）
  - **端到端が成立**: union輪郭 → Y反転 → ufoLib2 → fontmake OTF → uharfbuzz で組見本画像、まで実コードで貫通（spike3）
  - **物差し1本化が成立**: 同じ「十」を freetype 経路と poly 直描画経路で計測した差は縦2px/横1px、コントラスト比差 0.21%（spike3）
  - **KAGE の前提が成立**: ダンプ111MB取得可、常用漢字カバー率100%、行パースは24行。ただし **alias解決後も86.8%の字が部品参照(99)の再帰展開を必要とする（深度 max=5, mean=2.21）**。部品展開器は P4a の必須成果物（spike2）
  - 常用漢字2,136字・教育漢字1,026字のリストは PyPI `kanji-lists` で実カウント確認済み。`spike2/output/glyphset_joyo2136.txt` に試作を生成済み（本番の正本は `data/` へ凍結後）
  - **fontdb MVP の危険箇所は消化済み**（spike4）: 計測コーパス5書体すべて実取得成功（IPAex 含む。URL＋SHA256 は `spike4/corpus_actual.yaml` に確定。**全書体が静的フォントで、可変インスタンス化分岐は将来用**）。`san_uroko` は書体別チューニングなしで **5/5 成功**。散布図で伝統明朝クラスタ（コントラスト2.40〜2.50・うろこ相対0.84〜0.86）と BIZ UD（1.35・0.41）の分離を確認 — **デザインスペースの中間帯が空いている**ことが実測で判明
  - **黒み減衰カーブは弱い**（spike5）: 上記 v5 修正のとおり。P5 の主モデルを密度実測ベースへ変更
  - **交差ソルバは期待contour到達**（spike6→engine）: Stage A（k=0.15）＋微小除去（UPM²床 0.0035）。`tests/regression_join20.yaml` 20字×3 params グリーン（掟14）。曲線再適合は **RDP polyline 既定**（P2++）
  - **P4a→P3 接続は成立**（spike7）: KAGE展開骨格→写像→エンジン肉付けで代表10字すべて認識可能。常用100字の機械変換は成功率100%・約3.5ms/字（2,136字外挿 ≈7.5秒）、ハードフォールバック率 ≈4.5%。**製品品位の主ブロッカーは接合（P2）**であり、KAGE骨格自体の「読めない級」の歪みは軽微

### 0.1 座標系の統一（全レイヤー共通・違反禁止）

**内部正規座標は「フォント空間: Y上向き・UPM=1000」に統一する。**
- 骨格JSON・params・DB内の数値・UFO はすべてフォント空間
- prototype の SVG 出力時**のみ** Y 反転する（SVG は Y下向き）
- `render_profile.y_axis` は「ラスタ画像の行方向」だけを表すメタデータであり、設計座標の向きには使わない
- 既存 `prototype/` は SVG 座標（Y下）で書かれているため、エンジン本実装への移行時に骨格データを Y 反転して取り込む
- KAGE データは **200×200・Y下向き**。取り込み時に UPM=1000・Y上へスケール＋反転する

spike3 で実際にハマった点（実装時の必須手順）:
1. Y反転は `y' = UPM − y`。忘れると上下逆のフォントができる
2. **Y反転すると輪郭の巻き方向が逆転する**。CFF は塗り輪郭を反時計回り（正面積）に正規化してから UFO に書く
3. **全輪郭の一括反転は禁止**。打ち込み由来の微小島が「穴」扱いになり塗り欠けが起きる
4. 穴（カウンター）判定を bbox 内包だけでやると微小島を誤爆する。「口」「国」など真のカウンター字では入れ子判定（面積符号＋包含）を別途実装する

---

## 1. 全体構造と依存関係（v2 改訂: fontdb は全体のゲートではない）

**クリティカルパスは P1（仮名手設計）と P2（交差ソルバ）の並走**である。fontdb は「P0 の入力＋継続校正レーン」であり、fontdb MVP の完了を P1/P2 の着手条件にしない。

P0 に必要なのは fontdb 全体ではなく**計測スライス（T3ラスタ基盤＋T4 glyph計測＋probe 十/三）**だけ。散布図・bridge（T6/T7）は P0 と並行で閉じる。

```
[T0骨組み] → [T1取得] → [T2 DDL] → [T3 raster]
                              ↓
                    [T4 glyph_metric] → [T5α probe 十/三]
                              ↓
                    部分P0（太さ・うろこ初期値の実測）
                              ↓
              ┌───────────────┼───────────────┐
              ↓               ↓               ↓
         T6散布図        P2交差ソルバ       P1仮名手設計
              ↓         （skia導入可）          ↓
         T7 bridge※       P3製品化 ←────── 手設計UFO
              ↓               ↓
         校正ループ統合 ←── P0完成（製品候補1系統固定）
                              ↓
                    P4a→P4b→P4c 骨格整備 → P5量産 → P6校正・出荷

※ T7 は一時フォント化（union→OTF→freetype）経路が第一候補（§2.3。spike3 で実証済み）
```

---

## 2. トラックB: fontdb 構築計画

### 2.1 リポジトリ構成

```
myfont/
├── prototype/              # 既存実験場（正本にしない。実験SVGのみ）
├── fontdb/                 # DB本体
│   ├── pyproject.toml      # 依存グループ: [dev] / [engine]（skia-pathopsはengineのみ）
│   ├── schemas/schema.sql
│   ├── config/             # corpus.yaml / render_profiles.yaml / probe_defs.yaml
│   ├── data/fonts/         # 取得フォント（gitignore、URL+SHA256のみ管理）
│   ├── data/renders/       # 中間ラスタ（gitignore）
│   ├── data/db/fontdb.sqlite
│   ├── src/fontdb/         # acquire / render / metrics / probes / ingest / viz / bridge
│   ├── scripts/            # 01_fetch 〜 07_viz
│   ├── output/scatters/
│   └── tests/              # fixture＋黄金画像
├── engine/                 # エンジン本実装（P2以降。prototypeから移行）
├── fonts_out/              # UFO正本（git管理）＋build/（gitignore）
├── proofs/                 # 組見本（golden/ に凍結版）
└── docs/                   # measurement_protocol.md / kage_mapping.md / weekly.md
```

### 2.2 スキーマ（4層＋再現性管理）

- `family` → `face`（`face_kind`: opentype/ufo/svg_set/synthetic）→ `glyph_metric` → `probe_def`/`probe_metric`、＋`design_param_snapshot`
- 全レコードに `render_profile_id` + `extractor_version`。element層（ストローク自動分解）は作らない
- ストレージは SQLite 単体
- **extractor_version のバンプ規則**: 計測数式・ROI定義・二値化閾値のいずれかが変わったら必ず上げる。同一版の再実行は UNIQUE 制約で置換

### 2.3 計測条件（v2 改訂: render_profile は2系統）

**render_profile 戦略の正本はこの節**（他所の記述は要約。食い違ったらここに従う）。

外部書体と自作合成面ではラスタ経路が異なるため、**同一 profile を偽装しない**。

| profile | 対象 | 経路 |
|---|---|---|
| `ft_1024_nohint_gray_v1` | OTF/TTF/UFO由来 | freetype-py、hinting off、gray AA、閾値 T=128 |
| `poly_pillow_1024_gray_v1` | synthetic（接合前ポリゴン） | 頂点リスト→Pillowマスク描画 |

- 比較グラフには profile を凡例必須。**profile 混在の平均・回帰は禁止**
- 黒み密度・ふところ（穴）系 probe は **union（接合）後の face のみ `status=ok`**。接合前 synthetic は `low_confidence` 固定（重ね塗りは黒みが二重カウント・穴が偽になるため）
- **物差し1本化は spike3 で実証済み**: エンジン出力→一時 UFO/OTF 化→freetype profile 計測の経路が実コードで通り、poly 直描画との差は縦2px/横1px・コントラスト比差 0.21% と微小。**T7 は一時フォント化経路（freetype）を第一候補**とし、`poly_pillow_*` profile は union 前の暫定計測・デバッグ用フォールバックに格下げする

### 2.4 probe（v2 改訂: 2段階に分割）

**Phase α（MVP必須）**: `juu_contrast`（十のコントラスト）＋ `san_uroko`（三のうろこ、「二」フォールバック付き）。外部5書体＋synthetic（コントラスト系のみ）
**Phase β（接合実装＋骨格追加後）**: `kuni_futokoro`（国）＋ `kana_kanji_face`（あ/国）。「国」「あ」の骨格が自作側に無く、穴計測は接合後でないと意味を持たないため

失敗判定は fail / low_confidence / skipped。「うろこ無し」は失敗ではなく value≈0 の正常値（様式的ゼロと検出不能を混同しない）。

スパイクで確定したプロトコル追記:
- `juu_contrast` の走査線は**交点を回避する位置**で取る（交差部はインクが融合しラン長が汚染される）。プロトコルに「縦画計測は交点の上下、横画計測は交点の左右で取り、複数走査線の中央値を使う」を明記
- `san_uroko` は「水平投影ピーク検出→最上段横画→右端ROI→突出高さ」の手順＋**共通閾値**で、5書体すべて書体別チューニングなしに成功した（spike4）。BIZ UD は「fail しやすい」のではなく「小うろこの正常な外れ値」（value=0.41, ok）として扱う
- 実測アンカー（probe 実装のテスト期待値）: Noto Serif JP の十 ≈2.41。spike4 の5書体実測: 源ノ明朝 2.41/0.86、IPAex 2.50/0.85、しっぽり 2.46/0.86、Zen Old 2.40/0.84、BIZ UD 1.35/0.41（コントラスト比/うろこ相対）
- fontquant（googlefonts）のコントラスト計測は Latin グリフ（o/H/T/n）固定のため **T5α の代替にならない**。ink 密度系の実装のみ部分参考にする

### 2.5 可視化

散布図3枚（①字面率×黒み ②コントラスト×うろこ相対 ③仮名/漢字字面比×重心Y差）。③は Phase β 後。PCA・レーダーは指標が安定してから。

### 2.6 タスクリスト（MVP = T0〜T7、見積もり 50〜76h。spike4 でリスク消化後に下方修正）

spike4 で T1〜T6 相当の縮小版が既に動いている（`spike4/schemas/schema.sql`、`spike4/run_mvp.py`、`spike4/data/fontdb.sqlite`）。本実装はこの昇格＋整備が中心。

| ID | タスク | 完了条件 |
|---|---|---|
| T0 | venv＋`pyproject.toml`（依存グループ分離）・schema.sql・config | 空DB作成可、pytest 空回りで通る、`pip install -e ".[dev]"` 一発 |
| T1 | 5書体取得スクリプト | SHA256突合、ライセンス条文を corpus.yaml に固定、欠落時非ゼロ終了。**取得URL・SHA256 は spike4 で5/5確定済み**（`spike4/corpus_actual.yaml` を昇格）。現行5書体はすべて静的だが、**可変フォントを追加する場合は wght=400 にインスタンス化してから計測**（Noto Serif JP のデフォルトが ExtraLight(200) である事故を確認済み） |
| T2 | DDL適用＋シード | 冪等に再実行可能 |
| T3 | ラスタ化基盤（freetype-py） | 「十」1024px/EM PNG出力。hint on/off で画像ハッシュが変わることをテスト証明。**黄金画像＋許容誤差付き画素diffテスト** |
| T4 | glyph_metric 抽出 | 5 face × 約20字が status=ok、欠字は missing |
| T5α | probe 2種（十・三） | 合成fixtureの単体テスト合格、実書体の fail 率上限を yaml で固定 |
| T6 | 散布図（①②） | family＋profile 凡例付き PNG/SVG |
| T7 | prototype bridge | classic/modern を**一時フォント化（union→UFO→OTF）して freetype profile で計測・登録**（spike3 実証済みの経路）。union 前の暫定値が必要な場合のみ `poly_pillow_*` profile を併用し、profile を明示 |

### 2.7 技術選定

- fontdb MVP: freetype-py ＋ numpy ＋ Pillow ＋ fontTools（メタのみ）＋ matplotlib。**skia は入れない**
- エンジン（P2以降）: `pyproject` の `engine` グループに **skia-pathops** を追加。fontdb 側と依存を分離して「MVP依存最小」と「P2でpathops」を両立させる

### 2.8 第2期以降（番号は B系に改名。トラックAのP番号との衝突回避）

B1: ウェイト追加・自作代表字拡充（三・国・あ の骨格追加）→ B2: probe拡充（Phase β・はらいテーパー）→ B3: 黒みムラ空間分布・PCA → B4: KAGE/KanjiVG による ROI 自動提案

---

## 3. トラックA: フォント本体 制作計画

### 3.1 フェーズと完了条件（DoD）

| フェーズ | 内容 | 完了条件 |
|---|---|---|
| **P0 パラメータ校正基盤** | fontdb 計測スライス（T3+T4+T5α）の実測から初期値確定。字面規則・advance規則を文書化 | **達成（2026-08-09）**: `product_r1` frozen＋`design_param_snapshot`。字面規則は `docs/design_rules.md`。再生成は `engine/scripts/regen.py --params` |
| **P1 手設計核** | 仮名約170字＋基準漢字50〜100字 | 仮名・約物・数字が OTF で組める（**P3 以前は Glyphs 直接書き出しで可、と明示**）。固定文面の組見本を `proofs/golden/` に凍結し比較基準とする。**制作方式は P1-B（仮名パラメトリック生成、`docs/kana_parametric_plan.md`）を第一候補**とし、「手設計＝人間による設計判断（骨格YAML）」と解釈する。し＋つの品質到達に40h超で Glyphs ハイブリッドへ撤退 |
| **P2 交差ソルバ** | 接合整形＋単一輪郭化 | join20 グリーン。曲線再適合は **rdp_polyline 既定**（cubic は非デフォルト据え置き） |
| **P3 エンジン製品化** | 骨格JSON→肉付け→接合→UFO→fontmake | 手設計字とエンジン字が同一UFOに同居。**FontBakery universal ＋ 自己交差チェック（pathops または AFDKO checkoutlinesufo。FontBakery は自己交差を十分カバーしないため別建て必須）＋ cmap欠字チェック（glyphset差分）を通過**。manual_glyphs.txt の上書き検出を CI 相当で自動化。**最小パイプ（union→Y反転→UFO→OTF）は spike3 で実証済み**であり、P3 の残作業は品質ゲートと運用整備 |
| **P4a 骨格変換器** | KAGE→内部形式の変換器＋100字品質レポート | 成果物3点: ①行パーサ（実測24行で済む）②**部品展開器（alias解決＋再帰展開。常用の86.8%が必要、深度max=5。必須）** ③KAGE型→StrokeKind/EndTag の**写像仕様書（docs/kage_mapping.md）**。品質ゲートは**2段に分割**: 接合前ゲート（認識可能・写像正しさ。spike7 で100字クリア済み）と接合後ゲート（製品品位。P2 完了後）。工数目安 40〜80h（spike2/7 実測で妥当性確認済み。機械変換は軽く、仕様書＋目視が本体） |
| **P4b 頻出300字手修正** | 層C: 製品印象の8割 | 頻出300字が組見本で違和感閾値以下（1字30〜90分 ≒ **150〜450h の独立予算**として管理） |
| **P4c 残り一括変換** | 層A自動補正＋層B部品差し替え | 2,136字変換済み＋自動品質スコア |
| **P5 量産** | 部品合成＋黒み調整（**密度実測検知＋例外リスト主体。画数→線幅の一律減衰式は使わない**。縦画のみ弱い減衰 α≈0.07 を従属初期値に。spike5 実測に基づく） | 約2,500字がUFOに揃い、黒み異常の未解決が**全字の5%以下** |
| **P6 校正・出荷** | 組見本サイクル、name/OS/2メタデータ、fsType、家族名衝突確認 | 組見本3種で「違和感字リスト」が空 or 既知問題として文書化。リリース候補タグ |

並行ルール: P1∥P2∥fontdb。**P2 の DoD 未達のまま P5（量産）に入ることを禁止**。

### 3.2 技術スタック（確定）

- エンジン: Python 自前実装を継続。単一輪郭化は **skia-pathops（union）**
- **ポリゴン→ベジェの再適合ゲート**: **決定（2026-08-09）** — デフォルトは `rdp_polyline`（ε=1.5 UPM、Hausdorff ゲート同値）。UFO は簡略化折れ線。フル cubic は角・うろこ崩壊リスクのため M2 非デフォルト（`engine/snapshots/curve_refit.yaml`）
- ビルド: UFO ＋ fontmake。手設計字は Glyphs で描き UFO エクスポート、以降 UFO が正本。`manual_glyphs.txt` で上書き禁止をツール的に強制
- 組見本生成: **全面自作しない**。第一候補は **uharfbuzz / hb-view**（固定文面→PNG、黄金画像向き。spike3 で動作実証済み）。`diffenator2 proof` は Python 3.14 環境で `pkg_resources` 欠落により失敗した実績があるため、採用するなら Python 3.12 系の別 venv で検証してから。`scripts/make_proofs.py` は「固定文面の投入＋`proofs/golden/` 比較」だけの薄いラッパにする
- 文字セット定義: `data/glyphset_joyo2136.txt` 等のファイルを正とする。**リスト自体は自作せず Unihan の `kJoyoKanji` フィールド（または PyPI `kanji-lists`）から生成して凍結コミット**。教育漢字1,026字も同様。`.notdef`・グリフ名規則（`uniXXXX` 形式）・cmap はこのファイルから生成し、cmap 構築は fontmake/ufo2ft に任せる。**スコープ撤退はこのファイルの変更として明示コミット**
- 生成物 OTF は git 管理しない

### 3.3 交差ソルバの方針（2段構え。スパイクで実証済み）

スパイク結果（`spike/SPIKE_REPORT.md`）: union は交差の融合に有効（十: 6→2、永: 13→4 contour）だが、**union 単体では単一輪郭化しない**。残る原因は (1) 点画が他画と非接触 (2) 打ち込み由来の 10〜30 ユニットの微小島。よって Stage A と微小輪郭除去は必須工程として確定。

- **Stage A（骨格レベル）**: 交差を「十字 / T字 / 角 / はらい×横」に分類し接続タイプを付与。食い込み量は設計パラメータ。**spike6 実測: k=0.15 が最良（0.10 / 0.30 はどちらも「永」で悪化。過大な食い込みはうろこ島を生む）**。T字は「端点が他ストロークの近傍」で自動検出可能（永で4ヒット）。十字交差は食い込み不要で微小除去が本体。**非接触で正しい画（点など。骨格ギャップ≈49ユニット）は融合対象にしない**
- **Stage B（アウトラインレベル）**: 端物を本体に食い込ませてから pathops.union（API は `union(contours, outpen)`）→ **微小輪郭除去** → 微小線分除去・共線簡略化 → 交差内側角の局所整形（`join_inner_cut`）
- **微小輪郭除去の閾値は2段構え**（spike6 実測）: ink面積比 0.5% だけでは不足（classic「二」の島≈420 units² が残存）。**UPM² 床（0.08% ≈ 800 units²）＋「他輪郭と近接する微小片のみ除去」の近接判定**を併用する
- **P2 DoD に「期待 contour 数の一致」を含める**（十=1、二=2、永=点独立で2）。「破綻ゼロ」だけでは微小島を見逃す。**join20 は達成済み**。曲線再適合ゲートも **済（rdp_polyline）**
- 実装順: ~~engine 昇格 → 本・田 → join20 → 曲線再適合方針~~ **済**
- 撤退ライン: 手動例外が5字超かつ原因が曲線オフセット自己交差の連続 → オフセット方式を再設計

### 3.4 骨格データ戦略（KAGE 品位問題への4層対処）

| 層 | 対象 | 手法 |
|---|---|---|
| A 一括幾何補正 | 全2,136字 | 字面ボックス合わせ、重心の弱いスプリング補正、画間最低すき間 |
| B 部品テンプレ | 高頻度の偏旁冠脚 | さんずい・木偏・ごんべん等を手調整部品に差し替え |
| C 頻出字手修正 | 上位300字 | 骨格点を直接修正（P4b として独立予算管理） |
| D 低頻度は許容 | 残り | 自動補正のみ。組見本で目立つものだけ C へ昇格 |

KAGE由来骨格には変換パイプラインIDとソースリビジョンを必ず記録する。

spike7 の結合検証で判明した実態:
- KAGE骨格＋自作明朝ディテールの組み合わせは、**接合前の重ね塗り状態でも代表10字すべて認識可能**（永・国・十・木・東・語・人・山・口・海）
- 「読めない級」の骨格歪みは軽微で、**品位の主ブロッカーは接合（P2）**。層C（頻出300字手修正）は「読めるようにする」ためではなく「品位を上げる」ための工程と位置づけ直す
- 残る品質課題: 囲み字の右上うろこ過大、点・複曲線の近似精度、KAGE端点タグ0の補完規則（写像仕様書に含める）
- スケール実測: 常用100字の展開→写像→肉付けは成功率100%・約0.35秒。**2,136字の機械変換は ≈7.5秒**で、量産のボトルネックは計算時間ではなく品質確認

流用監査＋spike2 実地検証の結果:
- ダンプは111MB・取得可能・**常用漢字カバー率100%**（実測）
- 行パース（`name|related|data` の `|` 区切り）は**実測24行**で自前実装確定
- ただし「パースできれば骨格が手に入る」は誤り: 表面上ほぼ全字が alias（`uXXXX`→`uXXXX-j`）で、**alias 解決後も 86.8%（1,854/2,136字）が部品参照(99)の再帰展開を必要とする**（深度 max=5 / mean=2.21 / p95=3、展開失敗0）。**部品展開器が P4a の本体**
- KAGE 座標は 200×200・Y下向き → UPM1000・Y上へ変換必須（§0.1）
- kage-engine（kurgm 本家 JS / HowardZorn の Python ポート）は **GPL-3.0 のため依存・移植禁止**。描画仕様の参照のみに使う
- **KAGE型→StrokeKind/EndTag の写像と品質ゲートに既存実装は存在しない**。P4a の自作は正当（車輪の再発明ではない）。筆画タイプ×端点タグの対応表の下書きは `spike2/output/kage_mapping_table.md` にあり

### 3.5 手設計の順序

1. ひらがな核心約20字 → 2. ひらがな残り＋濁半濁・小書き → 3. カタカナ → 4. 約物 → 5. 英数
6. 基準漢字: 永・十・二・木・本 → 田・日・目・言・東 → 職・議・鬱 → 海・詩・村・国

ラフは紙/iPad、清書は Glyphs → UFO。手設計字からうろこ角度・打ち込み深さを逆計測して params にフィードバック。

### 3.6 品質管理

- 黒みムラ自動検出 → 例外リスト `data/density_exceptions.yaml`（**閾値を数値で記載**）
- 組見本3種（仮名本文／漢字交じり／ストレス字羅列）＋ `proofs/golden/` の凍結版と比較
- 回帰の期待結果はすべて黄金ファイル化（黄金画像・contour数・輪郭ハッシュ）し、更新はバージョン付きコミットで行う
- 進捗レビュー: **隔週15分**、`docs/weekly.md` に「M1デモ可能日／M2デモ可能日／今週の一手」の3行だけ必須（形骸化防止のため最小化）

### 3.7 マイルストーンと規模感（v2 で見積もり改訂）

| ID | 状態 | 規模感 |
|---|---|---|
| **M0** | fontdb MVP（T0〜T7） | **50〜76h**（spike4 でリスク消化・コード下敷きあり） |
| **M1** | 仮名だけのフォントで文章が組める | 150〜300h（**P1-B 方式採用時は 170〜520h・中央305h**。`docs/kana_parametric_plan.md` §6。回収は残り150字＋ウェイト展開） |
| **M2** | 交差ソルバ実証（回帰20字グリーン） | **120〜250h**（ベジェ再適合含む。旧見積もり80〜150hは楽観として棄却） |
| **M3** | 基準漢字＋エンジン初期量産 200〜400字 | 200〜400h（＋配管コストを含む） |
| **M4a** | 頻出300字の手修正完了 | **150〜450h**（M4から独立） |
| **M4b** | 常用漢字フル収録のα | 300〜600h |
| **M5** | β〜公開候補 | 200〜400h |

合計オーダー **1,200〜2,500h**。M5 の成功定義は「常用が欠字なく、仮名が品位を持ち、交差が壊れない個人公開レベル」。モリサワ級は方向目標であり合否基準にしない。

### 3.8 リスクと撤退基準

| リスク | 判断ポイント | 転換策 |
|---|---|---|
| 交差ソルバ不収束 | M2予算1.5倍（≈300h）でDoD未達 | 直線のみ自動・曲線は手修正部品、または合成フォント＋独自仮名へ縮小 |
| KAGE品位不足 | P4a の100字品質ゲート不合格、または M4b αで漢字だけ違和感 | 頻出500字まで手修正拡大、または教育漢字1,026字に縮小 |
| 仮名が弱い | M1組見本で歩行が悪い | エンジン開発を止めて P1 に回帰（例外リスト増で誤魔化さない） |
| 黒み例外爆発 | 例外が全字の15%超 | 規則不足とみなしコントラストを下げた現代寄りへ退避 |
| リソース枯渇 | マイルストーン間の空白90日 | M1（仮名フォント）を単体出荷して価値を確定 |

スコープ撤退の段階: ①常用2,136維持 → ②教育漢字1,026＋仮名英数 → ③仮名＋約物＋数字のみ公開 → ④エンジン研究として公開しフォントは中止。撤退は `data/glyphset_*.txt` の変更として明示的に行う。

---

## 4. 直近90日のバックログ（v2 で順序確定）

1. **T0〜T6**: fontdb 本実装 — **完了（2026-08-09）**。`fontdb/` パッケージ化、5書体計測・probe・散布図まで実行確認済み
2. **部分P0**: 実測中央値から `product_r1` 仮固定 — **完了（2026-08-09）**。`engine/params/product_r1.yaml`＋`PARAM_SETS["product_r1"]`（h=45 / v=110、contrast≈2.44）
3. **P2 昇格**: spike6 → `engine/` — **完了（2026-08-09）**。座標は svg_y_down_legacy（書き出し時 `y_for_font`。一括移行は肉付けと同時のみ）
4. **T7**: 一時フォント bridge — **完了（2026-08-09）**。`engine.bridge`＋十の freetype 計測 classic≈2.22 / product_r1≈2.43
5. **P2 深化（本・田）**: — **完了（2026-08-09）**。本=短横400–600・右端うろこ無し、`upm_area_ratio=0.0035`
6. **P2+ join20**: — **完了（2026-08-09）**。`tests/regression_join20.yaml` 20字×3 params グリーン（掟14）
7. **P2++ 曲線再適合**: — **完了（2026-08-09）**。既定=`rdp_polyline`（cubic 非デフォルト）。`engine.curve_refit`＋bridge 接続、節点削減＋誤差ゲート
8. **T7+: face 登録**: — **完了（2026-08-09）**。`fontdb.ingest.synthetic`＋`config/synthetic_faces.yaml`。classic/product_r1 を `face_kind=synthetic` で SQLite 登録（juu≈2.22/2.43、san_uroko ok）
9. **P0完成**: — **完了（2026-08-09）**。`product_r1` を `status=frozen` で正式固定。`design_param_snapshot`＋`face_param_link`、字面規則 `docs/design_rules.md`、`engine/scripts/regen.py --params`
10. 以下を実行（次フェーズ。優先順は §7.4 に従う）:
   - **P1着手（最優先・唯一の Go/No-Go ゲート）**: ひらがな核心20字＋の＋UI16字 — **方式A 手描き済。P1 縮小盲検 α Go（2026-08-17）**。次は `docs/quality_to_alpha_plan.md`
   - **S4 組見本ラッパ＋盲検プロトコル**: — **骨組み完了（2026-08-10）**。`scripts/make_proofs.py`＋`proofs/texts/{ui,hud,literary}.txt`＋`docs/blind_test.md`（主＝UI/HUD）
   - **S1 出荷ゲートCLI の骨組み**: — **骨組み完了（2026-08-10）**。`data/glyphset_*.txt` 凍結済、`engine/scripts/ship_gate.py`＋`docs/ship_gate_rules.md`
   - **S2a ブランド前提確認**: — **雛形作成（2026-08-10）**。`docs/licenses/precheck.md`（商標調査・GlyphWiki再確認は未実施チェックリスト）
   - **P4a本番は P-Q5（荒いクローズ）後**（§7.3）。cubic 再適合 opt-in は問題が出てから
11. 隔週レビュー（docs/weekly.md、3行のみ）

---

## 5. 流用マップ（v3 追加: 車輪の再発明監査の結果）

各コンポーネントの「自作 / 既存流用 / ハイブリッド」判定。**自作と判定されたものは監査の結果「既存が存在しない」ことを確認済み**であり、正当な自作。

| コンポーネント | 判定 | 流用先 / 根拠 |
|---|---|---|
| T3 ラスタ基盤 | 自作（薄い） | freetype-py の上に自前プロトコル固定層 |
| T4 glyph_metric | ハイブリッド | ink密度は fontquant / fontTools StatisticsPen を参考。ROI・欠字statusは自作 |
| T5α probe（十・三） | **自作（実証済み）** | fontquant は Latin グリフ固定。CJK明朝probe の既存実装は存在しない。spike4 で5書体無調整成功 |
| 組見本生成 | **既存流用** | **uharfbuzz / hb-view 第一候補**（spike3 実証済み）。diffenator2 は Py3.14 で失敗実績、採用時は 3.12 系別 venv。自作は黄金比較ラッパのみ |
| フォントQA | ハイブリッド | FontBakery universal ＋ 自己交差は pathops/checkoutlinesufo（FBは自己交差を十分カバーしない） |
| UFO構築・cmap | **既存流用** | ufoLib2 / fontmake / ufo2ft。グリフ名は `uniXXXX` 形式 |
| 常用漢字リスト | **既存流用（検証済み）** | PyPI `kanji-lists` で JOYO=2,136 / KYOIKU=1,026 を実カウント確認。生成済みファイルが `spike2/output/glyphset_*.txt` にあり |
| KAGE行パース | 自作（実測24行） | フォーマットが単純。GPL の kage-engine には依存しない |
| KAGE部品展開器 | **自作（必須）** | alias解決＋再帰展開（深度max=5）。常用の86.8%が必要。既存の非GPL実装なし |
| KAGE→内部形式写像 | **自作** | 既存実装なし（P4a の核）。対応表下書き: `spike2/output/kage_mapping_table.md` |
| 可変幅ストローク肉付け | **自作** | 「中心線＋可変幅ベジェ」の汎用ライブラリはエコシステムに存在しない（beziers.py は定幅のみ、Skia の可変幅strokerはC++サンプル止まり） |
| 単一輪郭化 | **既存流用** | skia-pathops（スパイクで動作実証済み） |
| 黒み密度・例外検出 | **自作** | CJK字内の黒みムラ検出OSSは見つからず。**方式は spike5 実測に基づき「密度実測検知」主体**（画数減衰式は不採用） |

---

## 6. ライセンス原則

- 参照書体は**計測のみ・輪郭トレース禁止**。疑義字形は作り直し
- 計測コーパスはオープンライセンスのみ。フォントバイナリは git に入れない（URL＋SHA256管理）
- コーパス追加時はライセンス条文確認を corpus.yaml エントリの完了条件にする
- KanjiVG（CC BY-SA 3.0）の形状は成果物に混入させない
- **GPL コード（kage-engine 本家・Python ポート）をリンク・移植しない**。GlyphWiki のダンプデータのみ利用する
- KAGE 骨格は変換・補正の由来を記録する
- ROI デバッグ画像等の公開方針は公開前に確認する

---

## 7. 事業整合ロードマップ（v7 追加）

`docs/strategy.md` のリリースフェーズ（α→β→v1.0）を既存フェーズ（P1〜P6・T系・M系）へ接続する。**エンジニアリングの依存関係（§1）は変えない**。戦略が新たに要求する成果物を S 系タスクとして追加し、各リリース版の出荷条件に紐付ける。

### 7.1 S系タスク（戦略由来の新規成果物。Grok 4.5 レビューを反映）

| ID | タスク | 実装先 | 完了条件（DoD） |
|---|---|---|---|
| **S0 販売オペ1枚** | 「売れる箱」の事業要件 | `docs/sales_ops.md` | 特商法表記・返金ポリシー・価格（税込）・BOOTH手数料前提・決済/インボイス方針を1枚に固定。**β出荷の必須条件**（法務全文 S2b より先にこれ） |
| **S1 出荷ゲートCLI** | OTF/UFO を入力に品質検査を一括実行 | `engine/scripts/ship_gate.py`（fontdb の計測とは独立の合否ゲート） | **着手条件: `data/glyphset_*.txt` の正本凍結**（未凍結だと cmap 差分がごまかせる）。検査項目: ① FontBakery universal サブセット（採否ルールを先に `docs/ship_gate_rules.md` に固定。全ゼロは非現実）② 自己交差（pathops）③ cmap欠字（glyphset 差分）④ OS/2・hhea の縦方向メトリクス（asc/desc）が `docs/design_rules.md` の 880/−120 と一致 ⑤ `.notdef` 存在 ⑥ name テーブル（家族名/スタイル/UniqueID/著作権/ライセンス文字列）⑦ OS/2 `fsType`（方針は S2 と整合させ文書固定）＋ `ulUnicodeRange`/`ulCodePageRange` の誤ビット禁止 ⑧ GSUB/GPOS は α/β では**未搭載でよい**（未搭載であることの確認のみ）。α版OTFが通過し、非ゼロ終了でCI相当に組込可能 |
| **S2a ブランド前提確認（α前・薄い）** | 名前と素材の販売可否 | `docs/licenses/precheck.md` | 家族名の商標ざっくり調査（J-PlatPat検索記録）＋GlyphWiki データ利用条件の**販売時**再確認（1ページ）＋α配布用の免責一文。**ブランド確定前ブロッカー**であり販売準備ではない |
| **S2b ライセンスキット（盲検後）** | 販売用ライセンス条文＋可否アイコン表 | `docs/licenses/`（personal.md / game_embed.md / maintenance.md / MATRIX.md） | L1個人・L2組込・保守の3条文＋可否マトリクス（商用ロゴ/組込/映像/再配布/改変/学習投入）。FAQ に Unity の同梱可否と TextMeshPro 前提を明記（Godot/Unreal は「未検証」と正直に書く）。**法務レビュー前は「草案」ラベル必須** |
| **S3 設計来歴カード** | 購入者向け来歴証明の自動生成 | `fontdb/scripts/`（採番は実装時） | **上半分は人間向けの3断言**（①参照フォント非トレース ②GPL非リンク ③KAGE由来＋変換パイプラインID）で固定し、params SHA256・コーパスURL/SHA256・ソースリビジョンは付録に回す。**1枚PDF上限**。`product_r1` で生成でき S5 の同梱物になる |
| **S4 組見本＋盲検プロトコル** | 「顔」の合否判定の仕組み | `scripts/make_proofs.py`（uharfbuzz 薄ラッパ、§3.2 既定）＋`proofs/golden/`＋`docs/blind_test.md` | 組見本3種を固定文面で生成。**主＝短文UI／ゲームHUD風**（事業ポジションに一致）、**副＝文芸本文（参考観測）**。盲検設計: 評価者3名中ゲーム/同人制作者≥2・提示順ランダム・比較対象書体はファイル名で固定・合否は面ごとに記録。**α の Go/No-Go は「UI≥2/3 かつ HUD≥2/3」**（本文の合否は Go/No-Go に使わない） |
| **S5 リリースパッケージング** | 配布物の自動組み立て | `scripts/package_release.py` | OTF＋EULA＋来歴カード＋CHANGELOG＋組見本PNG → zip。バージョン規則（`家族名-vX.Y.Z`）と CHANGELOG 書式（字数・修正理由）。**BOOTH用必須3点（カバー画像・組見本3枚・免責一文）も DoD に含む**。可変フォント／Webフォント配信は v1.0 まで非目標（スコープ膨張防止）。生成物は git 管理しない |
| **S6 ゲームエンジン組込検証** | L2販売の技術的裏付け | `docs/engine_embed.md`（β以降、必要なら検証プロジェクト） | チェックリスト固定: アトラス生成可否＋概算サイズ・動的テキスト・フォールバック連鎖・**サブセット前提の明記**・絵文字/外字の非対応・縦書き非対応（v1方針）。Unity TextMeshPro のみ実機確認、Godot/Unreal は「未検証」明示（削る） |
| **S7 ウェイト展開スパイク** | 第2ウェイト（v1.0 要件）の実現可能性 | `engine/params/product_r1_bold.yaml`（候補）＋既存 regen/join20 経路 | 同一骨格・params 変更のみで Bold 候補を生成し、join20 回帰＋盲検で「別書体に見えない」ことを確認。**Regular の品質ゲート通過前に着手しない**。対象は**静的第2ウェイトのみ**。可変フォント化の判断は「静的2ウェイトが盲検で同一家族と認定された後」 |

### 7.2 リリース版と出荷条件の対応

カレンダー目安（`docs/strategy.md` の 3–24ヶ月）は壁時計ではなく **t0＝α盲検合格日** を基準に読み替える。個人リソース前提で壁時計コミットはしない。

| 版 | エンジニアリング前提 | 戦略成果物 | 出荷条件 |
|---|---|---|---|
| **α（限定・無料デモ）** | P1核心（仮名一式＋基準漢字50〜100。Glyphs直接書き出し可＝P3不要） | S4（組見本＋盲検）、S1（ゲート骨組み）、**S2a（商標・GlyphWiki再確認・免責）** | 盲検 **UI≥2/3 かつ HUD≥2/3**・S1通過・「商用保証なし」明記 |
| **β（BOOTH販売開始・L1）** | P4a完了＋P3品質ゲート＋教育漢字1,026**パイプ**（変換が通ること） | **S0**・S2b・S3・S5 | S1フル通過・**公開字種は品位到達分（頻出上位 N≒200 目安）＋既知問題リストの公開**・公開スコープ内欠字ゼロ・返金ポリシー公開・**「常用完備」「教育完備」と宣言しない** |
| **v1.0（L2組込・L3法人）** | P4b（頻出300字）＋P4c＋P5＋P6（name/OS2/fsType） | S6・S7・保守ライセンス運用開始 | 常用2,136欠字ゼロ・黒み例外5%以下（P5 DoD）・組込FAQ公開 |
| **v2以降** | 縦書き（`vert`/`vrt2`）・ウェイトファミリー | セミカスタム受託の型化（SaaSは受託10件実証後） | — |

β の字種を「教育漢字フル販売」にしない理由: P4b（頻出手修正）前に教育フルを有料化すると「欠字ゼロだが品位未達のまま約束を売り切る」構造になる（失敗シナリオ2）。品位到達分だけ公開し、拡充は無償アップデートとして CHANGELOG に積む。

### 7.3 既存計画への影響（差分のみ）

- **P1 の位置づけを格上げ**: 「仮名完成＝事業の唯一の Go/No-Go ゲート」（`docs/strategy.md` 失敗シナリオ1）
- **P4a は荒いクローズ後に着手**: P1 仮名縮小盲検は済（2026-08-17 α Go）。P4a（40〜80h）は漢字交じり本盲検の合格を待たず、`docs/quality_to_alpha_plan.md` の P-Q5 後に着手可。仮名の顔が弱い間の本格量産はしない
- **P6 に S5/S2b を接続**: 出荷は「組見本サイクル＋メタデータ」に加えて「パッケージング＋ライセンス同梱」まで
- **M5（β〜公開候補）の成功定義に追記**: 従来の品質定義に加え「S0〜S5 が揃い BOOTH で販売可能な状態」
- **fsType（P6/S1⑦）**: 組込許可のビット設計はライセンス層（L1/L2）と矛盾しないよう S2b 確定後に最終化（αは Embeddable 方針を文書固定）
- **AI系機能はスコープ外を明示**: 雰囲気検索・タイポ生成API・マイグレーションAPIは v1.0 まで着手禁止。エンジンのパラメータ探索・欠字検知は内部ツールとしてのみ可

### 7.4 直近の優先順（次の一手）

正本: `docs/quality_to_alpha_plan.md`。

1. ~~P-Q0＋P-R1~~ — 診断表と `receive_manual.py` 済
2. ~~P-Q1~~ — づ・ん・や・を・る 受け入れ済（ink/20px 外れ解消）
3. ~~P-Q2~~ — 公式9字クローズ。る65・そ57は形状打ち止め（既知例外）。機械 `--apply` はしない
4. ~~P-Q3~~ — 濁点5字揃え。打ち込みは向き矛盾なしで触らず
5. ~~P-Q5~~ — 3名とも before が粗い。黄金 `kana_g3_blind_q5`
6. ~~S4 / S1~~ — 骨組み完了。**S2a** 調査実施が残（並走）。次は **P-E1**（ひらがな残り）。P-G1 並走可

P4a は **P-Q5 後**。α 本盲検（`ui.txt` / `hud.txt`）は欠字埋め後。S0/S2b/S3/S5 は本盲検合格後（合格前に販売準備をしない＝失敗シナリオ2の回避）。
